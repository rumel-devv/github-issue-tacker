Q.01 What is the difference between var, let, and const?
Ans: Var is the old way of declaring variables  before ES6.let was introduced in ES6 to solve the problems of var.const  declare variables whose value should not change.Var can be reasign and redeclare.
Let can't redeclared but reasigned.Const can't redeclared and reasigned.Var is Function scoped .But let and const is Blocked Scoped.

Q.02  What is the spread operator (...)?
Ans: Spread operator is a ES6 feature used to expand or spread elements of an array, object, or iterable into individual elements.It can copy and add array.

Q.03 What is the difference between map(), filter(), and forEach()?
Ans: Map  create a new array by applying a function to each element of the original array. It's
returns a new array but does'nt change originial array.

filter create a new array with elements that meet a specific condition.Returns a new array includes elements that pass the condition.

forEach loop through an array and run an action on each element.

Q.04 What is an arrow function?
Ans: Arrow function is a mordern and shorter way to write function introduced in ES6.

Q.05 What are template literals?
Ans: Template leterals morder way to write string. You can wirte multi line stiring in templte leterrials . It is a ES6 feature.



